
import { NavItem } from '@/types/navItem'
type Props = {
    navItem: NavItem[];
    scrollY: number;
    isOpen: boolean
}
const MobileNavbar = ({ scrollY, navItem, isOpen }: Props) => {
    return (
       <div>
        
       </div>
    )
}

export default MobileNavbar